[package]
name = "pdbinspector-wasm"
version = "0.1.0"
edition = "2021"
description = "WebAssembly bindings for PDB Inspector"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
pdbinspector = { path = "..", default-features = false }
wasm-bindgen = "0.2"
